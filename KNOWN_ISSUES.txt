KNOWN ISSUES==========================================================================================

. the grapple hook works, but under the hood is complete ass 
	-rewrite in progress

. the ui panel movement is horrible
	-fix: convert velocity vector to screen space vector

. grapple cube particles do not render properly
	-fix: instantiate new particles, enable them, and garbage collect them when done.

. if the player is able to remain inside of a hazard area (the damaging kind), they
will not take any damage
	-fix: change from area to StaticBody (why would the player ever need to swim in lava?)

. if a high number of decal scenes are requested from their pool and some become free, a null
casting error is thrown
	-fix: ensure decals are returned to the pool instead of freed?

. overlapping timeInterruptions can cancel each other out (for example, Dissipator special
and PistolBomb overlap), causing expected hitstop/slow interruption to fail
	-fix: queue/stack interruptions in TimeFlowSystem instead of replacing active interruption state

FIXED ISSUES==========================================================================================

. the pistol's special will extremely rarely not go back to normal state
	-FIXED: Removed pistol special

. grapple hook rarely collides with the world when it is in idle state untill it is
used again, then it fixes
	-FIXED: apparently, when scene heirarchy changes order, methods have to be called deferred or things
	get broken.

. player's velocity is brought down when attempting to move while already 
moving faster than speed limit
	-FIXED: Implemented smart aerial directional tracking: attempting to move in a new direction will 
	now consider the direction of current travel and attempt to conserve momentum instead of clamping down
	to the speed limit.

. enemies do not properly collide with the player on attacking 
	-FIXED: Reworked physics layer structure and allowed enemies to override their own physical collision
	rules.
